[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fiber-tracer"
version = "3.3.0"
description = "Regime-aware 3D fiber analysis for X-ray CT of fiber-reinforced composites"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Chandrashekhar Hegde", email = "hegde.g.chandrashekhar@gmail.com"}
]
requires-python = ">=3.12"
keywords = [
    "fiber analysis",
    "composite materials",
    "X-ray CT",
    "image processing",
    "structure tensor",
    "fiber orientation"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Image Processing",
]
dependencies = [
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "scikit-image>=0.21.0,<0.27.0",
    "pandas>=2.0.0",
    "matplotlib>=3.7.0",
    "tifffile>=2023.0.0",
    "imagecodecs>=2023.0.0",
    "h5py>=3.0",
    "pyyaml>=6.0",
    "tqdm>=4.65.0",
]

[project.optional-dependencies]
structure = ["structure-tensor>=0.2.0"]
skeleton = ["skan>=0.13.0"]
ml = ["torch>=2.0.0", "torchvision>=0.15.0", "scikit-learn>=1.3.0"]
unet = ["nnunetv2>=2.4.0; platform_system=='Linux'"]
tda = ["gudhi>=3.9.0"]
viz = ["matplotlib>=3.7.0", "plotly>=5.18.0", "napari>=0.4.18"]
parallel = ["zarr>=2.16.0", "dask>=2023.0.0"]
dvc = ["spam>=0.9"]
dev = [
    "pytest>=7.4.0",
    "pytest-cov",
    "black",
    "ruff",
    "mypy>=1.0,<3.0",
    "h5py>=3.0",
    "hypothesis>=6.0",
    "requests>=2.25",
]
all = ["fiber-tracer[structure,skeleton,ml,unet,tda,viz,parallel,dvc,dev]"]

[project.scripts]
fiber-tracer = "fiber_tracer.cli:main"

[project.urls]
Homepage = "https://github.com/Chandrashekhar-Hegde/fiber_tracer_2.0"
Documentation = "https://github.com/Chandrashekhar-Hegde/fiber_tracer_2.0#readme"
Repository = "https://github.com/Chandrashekhar-Hegde/fiber_tracer_2.0.git"
Issues = "https://github.com/Chandrashekhar-Hegde/fiber_tracer_2.0/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
fiber_tracer = ["*.yaml", "*.yml", "*.json"]

[tool.black]
line-length = 100
target-version = ['py312']

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
# Type-checking targets Python 3.12, the minimum supported runtime version.
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra -q --strict-markers"

[tool.ruff.lint.per-file-ignores]
# Config module uses typing.Optional/Union so runtime get_type_hints resolution
# stays robust; keep these annotations as-is rather than rewriting to X | Y.
"src/fiber_tracer/config.py" = ["UP007", "UP045"]
